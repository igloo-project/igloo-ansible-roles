---
argument_specs:
  main:
    short_description: Add systemd-timer
    options:
      timer_service_name:
        type: "str"
        required: true

      timer_unit_description:
        type: "str"
        required: true

      timer_oncalendar:
        type: "str"
        required: true
      
      timer_enable:
        type: "str"
        required: false

      timer_start:
        type: "str"
        required: false
