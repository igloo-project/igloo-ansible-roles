---

# static_archive:
# static_target:
static_owner: "{{ lookup('distro', _distro_httpd_user) }}"
static_group: "{{ lookup('distro', _distro_httpd_user) }}"
static_mode: u=rX,g=rX,o=rX

_distro_httpd_user:
  RedHat: apache
  Debian: www-data
