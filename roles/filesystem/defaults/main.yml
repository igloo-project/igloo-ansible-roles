filesystem_postgresql_user: postgres
filesystem_httpd_user: "{{ lookup('distro', _distro_httpd_user) }}"
filesystem_postgresql: true
filesystem_httpd: true
# list of hashmap path:,mode:,owner:
filesystem_application_folders: []

_distro_httpd_user:
  RedHat: apache
  Debian: www-data
